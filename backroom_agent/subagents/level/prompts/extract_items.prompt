你是后室（Backrooms）数据库的专家级数据分析师。
你的任务是分析提供的层级描述（HTML），并识别所有流浪者（Wanderer）可以**搜刮并放入背包带走**的**实用物品（Lootable Items）**。

对于发现的每个物品，请提供：
1. **名称 (Name)**：物品的名称（例如，“杏仁水”、“手电筒”、“生锈的钥匙”）。
2. **描述 (Description)**：基于文本的简要描述（例如，“一瓶散发着甜味的水。”，“旧的被摔坏的手电筒，可能需要电池。”）。
3. **ID**：一个唯一的、URL 安全的英文标识符，使用单数形式，小写，下划线分隔（例如，"almond_water", "flashlight"）。

请严格输出符合此 Schema 的 JSON 对象列表（包含 key `findable_items`）：
{{
  "findable_items": [
    {{
      "name": "string",
      "description": "string",
      "id": "string", // 英文，单数，小写，下划线
      "category": "string" // 例如 "Weapon", "Food", "Medical", "Tool", "Resource", "Document"
    }}
  ]
}}

- **只提取**：玩家可以直接捡起、放入背包并用于生存或后续冒险的物品（如食物、水、武器、工具、电池、医疗包、重要文件）。
- **处理量词后缀**：如果物品名称包含表示集合或堆积的量词（如“垛”、“堆”），请移除这些后缀。例如，“木材垛”应提取为“木材”，“一堆废铁”应提取为“废铁”。因为流浪者无法搬走整堆，只能拿走部分。
- **ID 生成**：
  - 这里请直接生成英文ID。
  - 必须为单数，除非像 "scissors" 这种本身就是复数的词。
  - 小写，空格转下划线。
- **严格排除**：
  - **仅仅是环境描述的一部分**：壁纸、地毯、天花板灯、插座、墙壁上的管道、霉菌、水坑。
  - **大型固定设施**：家具（椅子桌子）、机器、发电机、车辆。
  - **实体或生物**：任何活体或敌对生物。
  - **抽象或背景信息**：不要把概念性的东西当作物品。
  - **非在场物品**：如果文本只是在比较（例如“不像 Level 1 那样有杏仁水”），或作为传说/背景故事提到，但并未明确说明可以在**当前层级**找到或搜刮，则必须排除。
- 描述必须是客观和通用的。不要在描述中提及具体的层级或当前的上下文。
- 必须返回 JSON 对象，Root key 为 `findable_items`。
- 如果文本多次提及“杏仁水”，只需列出一次。
- **Name** 和 **Description** 必须使用 **简体中文**。
- **ID** 必须是英文，小写，用下划线分隔。

上下文：
{context}
