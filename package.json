{
  "name": "backroom-agent-root",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "install:frontend": "npm install --prefix frontend",
    "start": "npm run dev"
  }
}
