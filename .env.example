# Backroom Agent 环境变量配置
# 复制此文件为 .env 并填入实际的配置值

# ============================================
# LLM 提供商 API 密钥配置
# ============================================
# DeepSeek API 密钥（推荐，默认使用）
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# 豆包（Doubao）API 密钥（可选）
DOUBAO_API_KEY=your_doubao_api_key_here

# OpenAI 兼容 API 密钥（可选，作为 fallback）
# 如果 DEEPSEEK_API_KEY 或 DOUBAO_API_KEY 未设置，会使用此值
OPENAI_API_KEY=

# ============================================
# LLM 提供商配置（可选，使用默认值即可）
# ============================================
# DeepSeek 配置
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat

# 豆包配置
DOUBAO_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
DOUBAO_MODEL=ep-20241230123456-abcde

# ============================================
# Redis 配置（可选，用于缓存）
# ============================================
# Redis 主机地址（可选，默认: localhost）
REDIS_HOST=localhost

# Redis 端口（可选，默认: 6379）
REDIS_PORT=6379

# Redis 密码（可选，如果 Redis 没有设置密码则留空）
REDIS_PASSWORD=

# ============================================
# LangSmith 配置（可选，用于追踪和调试）
# ============================================
# 是否启用 LangSmith 追踪（可选，默认: false）
# 设置为 true 以启用 LangSmith 追踪功能
LANGCHAIN_TRACING_V2=false

# LangSmith API 密钥（可选）
LANGCHAIN_API_KEY=

# LangSmith 端点（可选，默认: https://api.smith.langchain.com）
LANGCHAIN_ENDPOINT=https://api.smith.langchain.com

# LangSmith 项目名称（可选，默认: backroom-agent）
LANGCHAIN_PROJECT=backroom-agent
