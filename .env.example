# Backroom Agent 环境变量配置
# 复制此文件为 .env 并填入实际的配置值

# ============================================
# OpenAI 配置（必需）
# ============================================
# OpenAI API 密钥 - 必需，否则 Agent 无法工作
OPENAI_API_KEY=your_openai_api_key_here

# OpenAI API 基础 URL（可选，默认: https://api.openai.com/v1）
# 如果使用其他兼容 OpenAI API 的服务，可以修改此值
OPENAI_BASE_URL=https://api.openai.com/v1

# 使用的模型名称（可选，默认: gpt-4o-mini）
OPENAI_MODEL=gpt-4o-mini

# ============================================
# Redis 配置（可选，用于缓存）
# ============================================
# Redis 主机地址（可选，默认: localhost）
REDIS_HOST=localhost

# Redis 端口（可选，默认: 6379）
REDIS_PORT=6379

# Redis 密码（可选，如果 Redis 没有设置密码则留空）
REDIS_PASSWORD=

# ============================================
# LangSmith 配置（可选，用于追踪和调试）
# ============================================
# 是否启用 LangSmith 追踪（可选，默认: false）
# 设置为 true 以启用 LangSmith 追踪功能
LANGCHAIN_TRACING_V2=false

# LangSmith API 密钥（可选）
LANGCHAIN_API_KEY=

# LangSmith 端点（可选，默认: https://api.smith.langchain.com）
LANGCHAIN_ENDPOINT=https://api.smith.langchain.com

# LangSmith 项目名称（可选，默认: backroom-agent）
LANGCHAIN_PROJECT=backroom-agent
